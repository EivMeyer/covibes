# Environment variables for ColabVibe frontend

# Backend API URL - can be overridden for different environments
VITE_API_URL=http://localhost:3001

# Alternative backend URL (takes precedence over VITE_API_URL if set)
# Use this for production deployments with custom domains
# VITE_BACKEND_URL=https://api.your-domain.com

# Frontend URL for CORS and OAuth callbacks
# Update this to match your frontend deployment URL
VITE_FRONTEND_URL=http://localhost:3000

# Production example:
# VITE_API_URL=https://api.colabvibe.com
# VITE_FRONTEND_URL=https://app.colabvibe.com